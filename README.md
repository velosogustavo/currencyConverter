# 💱 Convert — Conversor de Moedas

Uma aplicação web moderna e direta para converter moedas internacionais (Dólar, Euro e Libra) para o Real Brasileiro.

Desenvolvido em **HTML, CSS e JavaScript puro**, com foco em manipulação de formulários, formatação numérica (`Intl`) e interface responsiva.

## ✨ Funcionalidades

- 🔢 **Validação de Input:** O campo de valor aceita apenas números (bloqueio de caracteres de texto via Regex).
- 🌍 **Múltiplas Moedas:** Suporte para conversão de Dólar Americano (USD), Euro (EUR) e Libra Esterlina (GBP).
- 💲 **Formatação BRL:** Exibição do resultado formatado automaticamente no padrão brasileiro (R$ 0,00).
- 🎨 **Interface Dinâmica:** O rodapé com o resultado aparece suavemente apenas após a conversão.
- 💅 **Estilização Custom:** Design com tema escuro (Dark Mode) e scrollbar personalizada.

## 📂 Estrutura do Projeto

```text
/
├── index.html         # Estrutura da página e formulário
├── styles.css         # Estilização (Cores, fontes e layout)
├── scripts.js         # Lógica de conversão e manipulação do DOM
├── img/               # Assets (Logo, Background, Favicon)
└── README.md
```
## 🚀 Como executar

1. Baixe ou clone o repositório:

```bash
git clone [URL_DO_SEU_REPOSITORIO]
```

2. Abra o arquivo index.html diretamente no seu navegador.

...e a aplicação já estará pronta para uso!

## 🔧 Tecnologias usadas

- HTML5
- CSS3
- JavaScript Vanilla
- Google Fonts (Open Sans)